# Backstage configuration
# Override these in overlays/environments/<env>/backstage/values.yaml

backstage_image: "public.ecr.aws/seb-demo/backstage:cnoe"
git_username: ""        # Git username for repo access
working_repo: ""        # Working repository URL
gitlab_domain_name: "dfzsl5lc0kh4k.cloudfront.net"  # GitLab domain (defaults to ingress_domain_name if same host)

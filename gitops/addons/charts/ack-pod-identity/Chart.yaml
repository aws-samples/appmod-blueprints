apiVersion: v2
name: ack-pod-identity
description: ACK-managed IAM roles, policies, and EKS pod identity associations for platform addons (kro-ack provider mirror of crossplane-pod-identity)
type: application
version: 0.1.0

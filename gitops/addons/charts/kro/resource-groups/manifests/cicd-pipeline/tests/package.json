{
  "name": "cicd-pipeline-rgd-tests",
  "version": "1.0.0",
  "description": "Unit tests for CI/CD Pipeline Resource Group Definition",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:integration": "cd integration && npm install && npm run test:e2e",
    "test:all": "npm test && npm run test:integration"
  },
  "devDependencies": {
    "vitest": "^4.0.18",
    "js-yaml": "^4.1.0",
    "@types/js-yaml": "^4.0.5",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "mustache": "^4.2.0"
  },
  "type": "module"
}
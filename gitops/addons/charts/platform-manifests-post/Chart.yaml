apiVersion: v2
name: platform-manifests-post
description: Post-install platform manifests — HuggingFaceModel resources that require KRO RGDs to be processed first
type: application
version: 0.1.0
appVersion: "1.0"

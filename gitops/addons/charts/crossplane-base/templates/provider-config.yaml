{{- if .Values.providerConfig.enabled }}
apiVersion: aws.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: {{ .Values.providerConfig.name | default "default" }}
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
    argocd.argoproj.io/sync-wave: "2"
spec:
  credentials:
    source: PodIdentity
{{- end }}
{{- range $name, $provider := .Values.providers }}
{{- if kindIs "map" $provider }}
{{- if and $provider.version $provider.serviceAccount }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ $provider.serviceAccount }}
  namespace: {{ $.Values.namespace }}
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
    argocd.argoproj.io/sync-wave: "-1"
---
apiVersion: pkg.crossplane.io/v1beta1
kind: DeploymentRuntimeConfig
metadata:
  name: {{ $name }}-drc
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
    argocd.argoproj.io/sync-wave: "-1"
spec:
  deploymentTemplate:
    spec:
      selector: {}
      template:
        spec:
          serviceAccountName: {{ $provider.serviceAccount }}
          containers:
          - name: package-runtime
            resources:
              requests:
                cpu: {{ $.Values.providerResources.cpuRequest | default "250m" }}
                memory: {{ $.Values.providerResources.memoryRequest | default "1Gi" }}
              limits:
                memory: 2Gi
{{- end }}
{{- end }}
{{- end }}

# Apps

Application workload definitions. This directory is reserved for tenant application manifests that will be deployed to fleet member clusters.

Currently empty. As the platform matures, this is where application-level Helm charts, Kustomize overlays, or raw manifests will live -- separate from platform addons and infrastructure.

See the root [README.md](../README.md) for overall platform architecture.

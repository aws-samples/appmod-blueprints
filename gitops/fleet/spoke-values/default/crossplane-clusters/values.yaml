# Default values for fleet cluster provisioning.
# Add entries under 'clusters' to provision new fleet member clusters via Crossplane.
# Each key becomes the PlatformCluster claim name.
#
# Example:
# clusters:
#   spoke-us-west-2:
#     region: us-west-2
#     clusterName: spoke-us-west-2
#     vpcCidr: "10.1.0.0/16"
#     kubernetesVersion: "1.32"
#     autoMode: true

clusters: {}

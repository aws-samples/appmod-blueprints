# Workshop: pin argo-workflows (controller + server) to the tuned system-peeks pool.
# Generic default is the built-in `system` pool.
controller:
  nodeSelector:
    karpenter.sh/nodepool: system-peeks
server:
  nodeSelector:
    karpenter.sh/nodepool: system-peeks

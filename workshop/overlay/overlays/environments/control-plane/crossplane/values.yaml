# Workshop: pin crossplane (core + rbacManager) to the tuned system-peeks pool.
# Generic default is the built-in `system` pool.
nodeSelector:
  karpenter.sh/nodepool: system-peeks
rbacManager:
  nodeSelector:
    karpenter.sh/nodepool: system-peeks

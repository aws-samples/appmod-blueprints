# Workshop: pin the EKS ACK controller to the tuned system-peeks pool.
# Deep-merges onto deployment.nodeSelector (keeps the compute-type/os keys).
# Generic default is the built-in `system` pool.
deployment:
  nodeSelector:
    karpenter.sh/nodepool: system-peeks
